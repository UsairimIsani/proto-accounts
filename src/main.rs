use std::collections::HashMap;
use std::ops::Index;

fn main() {
    // let mut hashmap: HashMap<&str, i32> = HashMap::new();
    // hashmap.insert("something", 52);
    // let a = hashmap.keys();
    // println!("{:?}", a);
    // let b = "something";
    // let c = hashmap[&b];
    // let a = hashmap.values();
    // println!("{:?}", a);
    //
    //
    // let b = "something";
    // let c = hashmap[&b];
    // println!("{}", c);
    // hashmap.insert("tomatto", 90);
    // hashmap.entry("Mutton").or_insert(45);
    // hashmap.entry("zama").or_default();
    // hashmap.entry("tomatto").or_default();
    // println!("{:?}", hashmap);
    //
    //
    // let mut hashmap: HashMap<&str, Option<f64>> = HashMap::new();
    // hashmap.entry("No ").or_default();
    // println!("{:?}", hashmap);
    //
    //
    // let mut hashmap: HashMap<&str, Option<f64>> = HashMap::new();
    // hashmap.entry("No ").or_insert(Some(89.0000003));
    // let a_val_of_hsmp: &Option<f64> = hashmap.index("No ");
    // println!("{:?}", hashmap);
    // if let Some(i) = a_val_of_hsmp {
    //     println!("{:?}", i)
    // }
    //
    //
    // let vec_of_some: Vec<Option<i16>> = vec![Some(3), Some(2), None, Some(89), None];
    // let vec_of_str: Vec<&str> = vec!["hello", "world", "chicken", "kebab", "paratha"];
    // let hashmap: HashMap<_, _> = vec_of_str.iter().zip(vec_of_some.iter()).collect();
    // println!("{:?}", hashmap);
    // for (key, val) in hashmap.iter() {
    //     println!("{} {:?}", key, val);
    // }
}
